# Design System

## Theme

“开放研究笔记”：以开源文档站的阅读体验承载课题组信息，固定左侧导航、稳定正文宽度和清晰的学术列表是主体。量子态轨道只作为站点标记与局部解释性图形；南京大学紫用于导航状态与链接，洋红和电蓝仅作为少量辅助色。

## Color Palette

- `--ink`: oklch(22% 0.055 295) — 标题与正文
- `--muted`: oklch(47% 0.035 295) — 次级文本
- `--paper`: oklch(98.5% 0.006 300) — 主背景
- `--surface`: oklch(95.5% 0.018 302) — 分区背景
- `--violet`: oklch(43% 0.19 298) — 南京大学紫的现代化主色
- `--magenta`: oklch(58% 0.23 344) — 相位与关键交互
- `--blue`: oklch(66% 0.16 242) — 数据与状态辅助色

## Typography

中文使用系统宋体作为展示字，正文使用系统黑体；英文展示使用 Georgia，数据与公式使用等宽系统字体。大标题字距不小于 -0.035em，正文行宽控制在 72ch 以内。

## Components

- 顶部导航：半透明但不依赖装饰性玻璃效果，滚动后以实色纸面和细边界固定。
- 量子标记：三个相交轨道的原创 CSS 图形，用于站点识别。
- 研究轨道：非卡片化的横向研究条目，以编号、标题、说明和动态波形组织。
- 论文条目：完整边界分隔的文献列表，年份可快速跳转。
- 状态标签：仅用于内容类型和年份，圆角胶囊而非大面积圆角容器。
- 按钮：主按钮为深紫实底，次按钮为纯边框；不同时叠加宽阴影。

## Layout

桌面端使用 248px 固定左侧导航，可折叠至 76px；正文区最大宽度 1180px，长页面增加约 184px 页内目录。首页首屏同时呈现研究概览与交互式 Bloch 球。移动端在 820px 以下将侧栏折叠为顶部抽屉。

## Content Data & Languages

- 论文、成员与成员分类统一维护在 `assets/data/site-data.js`，首页、论文页和成员页只负责渲染，不重复保存条目。
- 中文页面位于站点根目录，英文页面位于 `en/`；侧栏底部的语言链接保持当前页面语境。
- 英文版与中文版共用论文和成员数据，成员分类名称及空状态分别提供中英文文案。

## Motion

首屏量子轨道持续低速旋转，概率点沿路径移动；指针只产生轻微视差。内容进入视口时使用短距离位移、裁切和模糊消退。所有持续动画在 `prefers-reduced-motion: reduce` 下禁用，内容始终默认可见。
